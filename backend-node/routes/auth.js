const router = require('express').Router()
// Placeholder auth — extend with User model as needed
router.post('/login', (req, res) => res.json({ message: 'Auth not configured' }))
module.exports = router
